---
- name: "Deploy Prometheus service"
  hosts: tag_web_stack_ansible_instance
  user: ubuntu
  become: true
  gather_facts: false
  vars:
    ansible_ssh_private_key_file: "~/.ssh/ec2-key-pair.pem"
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

  roles:
    - monitoring
